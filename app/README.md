# Makeup Telegram Bot

## Run locally
1) Create .env with:
BOT_TOKEN=...
TZ=Europe/Vienna
DAILY_HOUR=10
DAILY_MINUTE=0
DB_PATH=data.sqlite3

2) Install:
pip install -r requirements.txt

3) Run:
python -m app.main
